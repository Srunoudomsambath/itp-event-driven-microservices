spring:
  application:
    name: account
  profiles:
    active: dev
  config:
    import: optional:configserver:http://localhost:8888
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri:  http://localhost:9090

  datasource:
    driver-class-name: org.postgresql.Driver
    username: itpusr
    password: itp@168
    url: jdbc:postgresql://localhost:16168/db_account
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
axon:
  kafka:
    bootstrap-servers: localhost:29092,localhost:29094,localhost:29096
    default-topic: Axon.Account.Events
    client-id: kafka-axon-account
    message-converter-mode: default # alternative is cloud_event <https://cloudevents.io/>
    properties:
      security.protocol: PLAINTEXT # PLAINTEXT for no security purpose
    publisher:
      confirmation-mode: transactional
    producer:
      transaction-id-prefix: kafka-axon-account-txn
      retries: 5 # bigger than zero in case of using transactional confirmation-mode
      event-processor-mode: tracking
#    consumer:
#      event-processor-mode: subscribing

  axonserver:
    enabled: false
  serializer:
    general: jackson
    events: jackson
    messages: jackson

eureka:
  instance:
    instance-id: ${spring.application.name}
    prefer-ip-address: true
#customer:
#  service:
#    url: http://localhost:20260
server:
  port: 20261